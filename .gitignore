# Sinh ra bởi build/analyze — không bao giờ commit
build-test.rbxlx
build-ci.rbxl
*.rbxl
# ⚠️ NGOẠI LỆ: snapshot THẾ GIỚI GAME (File → Download a Copy trong Studio).
# Đây là bản sao lưu duy nhất của map nằm trong version control — mọi thứ .rbxl khác
# đều là sản phẩm build nên vẫn bị loại ở dòng trên. Chỉ file này được giữ lại.
!SpeedEscape.rbxl
sourcemap.json
sourcemap-check.json
lsp-out.txt
globalTypes.d.luau

# Media nặng, không phải source (đã upload lên Roblox rồi)
*.mp3
*.mp4

# Bản sao lưu place cũ — dùng Version History trên Creator Dashboard thay thế
*.backup-*.rbxlx

# Rác của công cụ
.claude/worktrees/
.codegraph/

# Bí mật cục bộ — API key Open Cloud. KHÔNG BAO GIỜ commit.
.env
.env.local

# Ghi chú công cụ nội bộ — giữ trên máy, không đẩy lên remote
CLAUDE.md
AGENTS.md
.mcp.json
