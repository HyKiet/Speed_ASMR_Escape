# SpeedEscape — điểm vào cho AI

## ⛔ ĐỌC `AGENTS.md` NGAY BÂY GIỜ, TRƯỚC KHI LÀM BẤT CỨ VIỆC GÌ

Toàn bộ luật kỹ thuật của repo — kiến trúc, khuôn mẫu, luật cứng, ngân sách hiệu năng,
quy trình publish — nằm trong **một file duy nhất: [`AGENTS.md`](AGENTS.md)**.

File này tồn tại chỉ để trỏ sang đó, vì Claude đọc `CLAUDE.md` còn Codex đọc `AGENTS.md`.
Cả hai phải làm việc theo **cùng một bộ luật**, nên chỉ có một bản nội dung.

**Đừng viết luật vào file này.** Có gì cần bổ sung thì sửa `AGENTS.md`.

Hai điều quan trọng nhất, nhắc trước ở đây để không ai bỏ sót:

1. Tra **https://github.com/Roblox/creator-docs** trước khi trả lời bất cứ câu hỏi nào về API
   hay hành vi engine Roblox. Dẫn được đường dẫn file trong đó thì mới là biết.
2. **Code xong / fix xong phải liệt kê đã tra những file nào** trong creator-docs, kèm rút ra
   được gì. Không tra thì ghi rõ là không tra và vì sao. Thiếu khối đó = việc chưa xong.
   Mẫu bảng ở `AGENTS.md` §0.3.
3. Rojo sync **một chiều** (VSCode → Studio). Sửa script trong Studio sẽ bị ghi đè mất.
